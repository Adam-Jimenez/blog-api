const mail = require('mail')
const errors = require('errors')

module.exports = {
    mailAdmin: (req, res) => {
        try {
            mail.mailAdmin(req.body.subject, req.body.content)
        } catch (err) {
            errors.handleRestError(req, res)(err)
        }
    }
}
